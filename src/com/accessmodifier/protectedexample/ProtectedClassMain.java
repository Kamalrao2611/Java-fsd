package com.accessmodifier.protectedexample;
import com.accessmodifier.*;

public class ProtectedClassMain {

	public static void main(String[] args) {
		ProtectedAccessSpecifier proAccessSpecifier = new ProtectedAccessSpecifier();
		// you can't access protected method outside a package
//		proAccessSpecifier.show();

	}

}